/**
 * Hold controllers which will be declared in angular automatically
 * @type {Object}
 */
var controllers = {

	/**
	 * Abstract controllers are not declared in angular
	 */
	abstract: {}
};

/**
 * Hold services which will be declared in angular automatically
 * @type {Object}
 */
var services = {

	/**
	 * Abstract services are not declared in angular
	 */
	abstract: {}
};

/**
 * Hold the application models
 * @type {Object}
 */
var models = {};

/**
 * Hold i18n locales objects, indexed by language ref
 * @type {Object}
 */
var locales = {};

/**
 * Hold the application directives
 * @type {Object}
 */
var directives = {};

/**
 * Hold the application filters
 * @type {Object}
 */
var filters = {};

/**
 * Hold various configuration objects used across the application
 * @type {Object}
 */
var config = {};
